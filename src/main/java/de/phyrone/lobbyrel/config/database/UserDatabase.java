package de.phyrone.lobbyrel.config.database;

public class UserDatabase {
	/*static AsyncMySQL sql = null;
	private void init() {
		if(sql == null) {
			sql = new AsyncMySQL(LobbyPlugin.getInstance(), Config.getString("MySQL.Host", "localhost"), Config.getInt("MySQL.Port", 3306), Config.getString("MySQL.User", "user"), Config.getString("MySQL.Password", "MeinPassword"), Config.getString("MySQL.Database", "database"));
		}try {
			sql.update(sql.getMySQL().getConnection().prepareStatement("CREATE TABLE IF NOT EXISTS autonick_players( uuid TEXT, data TEXT )"));
		} catch (SQLException e) {
			e.printStackTrace();
		}
			
	}public static void reset() {
		if(sql != null) {
			sql.getMySQL().closeConnection();
			sql = null;	
		}
	}
	public static void savetoDB(Player player) {
		
	}*/
}
